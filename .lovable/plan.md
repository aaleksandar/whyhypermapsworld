## Update Solution slide copy

Edit `SlideSolutionHero` in `src/components/deck/slides.tsx`:

- **Headline:** "A map that knows who you are — and the worlds you belong to."
  - Style the second clause (" — and the worlds you belong to.") in italic Fraunces so the two beats read distinctly.
- **Sub:** "Slip between worlds like rooms: foodies, skaters, modernists, night owls. Each one curated by the people already living it. The map listens, learns, and shows you the city you'd actually love."
- Keep the existing layered-worlds background animation and eyebrow ("the solution") untouched.
- Add 3 small floating "world" chips drifting behind the headline — labels: *Vegan*, *Architecture*, *Late-night* — using the existing paper/sticker style with subtle framer-motion drift, low opacity so they sit behind the type.

No other slides touched.
